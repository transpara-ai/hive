.env
.DS_Store
.hive/
.worktrees/
products/
*.exe
*.stackdump
hive.exe~

# Go built binaries
/hive
/approve-role
/adjust-budget
/user-api
/hive-ops-api

# Loop runtime state (regenerated every daemon cycle)
loop/daemon.status
loop/diagnostics.jsonl

# Operator-local MCP config variants (may carry values) — never commit (FO R5/D2)
loop/*.local.json
